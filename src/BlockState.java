

/**
 * Enum that stores the various possible states that a block can have 
 * (in terms of how the User has interacted with it)
 * @author vikramsingh
 */
public enum BlockState {
    
    UNCHECKED,
    
    FLAGGED,
    
    DISCOVERED
    
}